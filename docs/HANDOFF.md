# Handoff

Оперативный handoff больше не хранится на `main`.

Читать:

```text
PROJECT.md
→ active development branch
→ PROJECT.md on that branch
→ docs/CONTEXT.md on that branch
```

Это намеренно исключает дублирование текущего состояния между `main` и development line.
